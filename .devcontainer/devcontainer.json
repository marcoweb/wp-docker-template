{
    "postCreateCommand": "bash ./assets/scripts/updateurl.sh && docker-compose up -d && docker exec wordpress sh -c 'exec chown -R www-data /var/www/html/wp-content'; alias dbsave='bash ./assets/scripts/dbsave.sh'"
}